namespace GameOfLife;

public enum GameState
{
    Stopped = 0,
    Running = 1,
}


public enum GameButtonActions
{
    Nothing = 0,
    Exit = 1,
    StopGame = 2,
    StartGame = 3,
    RandomBoard = 4,
    ClearBoard
}